## Copy to `.env` in the project root (do not commit `.env`) and fill in your keys.
## Backend (Flask) reads these via python-dotenv.
YOUTUBE_API_KEY="YOUR_YOUTUBE_API_KEY"
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

## Frontend (Next.js) uses this to reach the backend.
## Local default is already http://localhost:5000, so this is optional for dev.
NEXT_PUBLIC_API_URL="http://localhost:5000"


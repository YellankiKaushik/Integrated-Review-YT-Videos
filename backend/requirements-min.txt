# Minimal dependencies to run the current `backend/app.py` locally.
# Use `backend/requirements.txt` only if you need the heavier ML stack.
flask==2.3.3
flask-cors==4.0.0
google-api-python-client==2.97.0
python-dotenv==1.0.0
openai==1.3.0
httpx==0.24.1
youtube-transcript-api==0.6.1

